//--------------------------------------------------------------------
// XLSP.AML
// Syntax highlighting for AutoLisp files (Lsp)
//
// After making changes, save this file and compile with <shift f10>.
//--------------------------------------------------------------------

include bootpath "define.aml"

syntax                            // options:
  'bcfn'                          //   b=show through marked block
                                  //   c=highlight cursor line
                                  //   d=show through closed folds
                                  //   f=use only foreground colors
                                  //   i=ignore keyword case
                                  //   n=highlight numbers
  '{}[]().=+-*/<>\t'              // symbol set 1
  ''                              // symbol set 2
  '"'                             // string characters
  '\\'                            // string literal char
  ''                              // numeric char
  ';'     0                       // eol comment 1 / start column
  ''      0                       // eol comment 2 / start column
  ''      ''                      // multi-line comment 1
  ''      ''                      // multi-line comment 2
  0                               // number of lines to scan backward

  // colors
  color brightcyan   on black     // keyword
  color gray         on cyan      // symbol set 1
  color gray         on cyan      // symbol set 2
  color brightred    on black     // string
  color brightred    on black     // numeric
  color brightgreen  on black     // eol comment 1
  color brightgreen  on black     // eol comment 2
  color brightgreen  on black     // comment 1
  color brightgreen  on black     // comment 2

keyword
  abs, ads, angle, angtos, append, apply, ascii, assoc, atan, atof,
  atoi, atom, boole, boundp, cadar, cadr, car, carr, cddr, cdr, char,
  chr, close, command, cond, cons, cos, cvunit, defun, distance, entdel,
  entget, entlast, entmakee, entmod, entnext, entsel, entupd, eq, equal,
  eval, exp, expt, findfile, fix, float, foreach, gcd, getangle,
  getcorner, getdist, getenv, getint, getorient, getpoint, getreal,
  getstring, getvar, getword, graphscr, grclear, grdraw, grread, grtext,
  handent, if, initget, inters, itoa, lambda, last, length, line, list,
  listp, load, log, logand, logior, lsh, mapcar, max, member, menucmd,
  min, minusp, nentsel, not, nth, null, numberp, open, or, osnap, pi,
  polar, princ, print, print1, progn, prompt, quote, read, redraw,
  regapp, rem, repeat, reverse, rtos, set, setq, setvar, sin, sqrt,
  ssadd, ssdel, ssget, sslength, ssmemb, ssname, strcase, strcat,
  strlen, subst, substr, tblnext, tblsearch, terpri, textpage, textscr,
  trace, trans, type, untrace, ver, vport, wcmatch, while, write,
  xdroom, xdsize, xload, xunload, zerop
